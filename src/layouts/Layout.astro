---
import '../styles/global.css';
import Footer from '../components/Footer.astro';
import LanguageSwitcher from '../components/LanguageSwitcher.astro';
import Wordmark from '../components/Wordmark.astro';
import { siteConfig } from '../data/site';

interface NavLink {
  href: string;
  label: string;
}

interface Props {
  title: string;
  description: string;
  lang?: 'de' | 'en';
  path: string;
  alternatePath?: string;
  nav?: NavLink[];
}

const {
  title,
  description,
  lang = 'de',
  path,
  alternatePath,
  nav = [],
} = Astro.props;

const t = lang === 'de'
  ? { skip: 'Zum Inhalt', home: 'KyleHub Startseite', nav: 'Hauptnavigation' }
  : { skip: 'Skip to content', home: 'KyleHub home', nav: 'Primary navigation' };
const siteUrl = siteConfig.url;
const canonical = new URL(path, siteUrl).toString();
const alternate = alternatePath ? new URL(alternatePath, siteUrl).toString() : null;
const homeHref = lang === 'de' ? '/' : '/en/';
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#181f1d" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="canonical" href={canonical} />
    {alternate && <link rel="alternate" href={alternate} hreflang={lang === 'de' ? 'en' : 'de'} />}
    <link rel="alternate" href={canonical} hreflang={lang} />
    <link rel="alternate" href={lang === 'de' ? canonical : alternate ?? canonical} hreflang="x-default" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="KyleHub" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <title>{title}</title>
  </head>
  <body>
    <a class="skip-link" href="#main">{t.skip}</a>
    <div class="page-shell">
      <header class="site-header">
        <div class="site-header__inner">
          <Wordmark href={homeHref} ariaLabel={t.home} />
          <nav class="site-nav" aria-label={t.nav}>
            {nav.map((item) => (
              <a
                href={item.href}
                class:list={{ 'site-nav__link': true, 'site-nav__link--active': item.href === path }}
                aria-current={item.href === path ? 'page' : undefined}
              >
                {item.label}
              </a>
            ))}
          </nav>
          <LanguageSwitcher lang={lang} alternatePath={alternatePath} />
        </div>
      </header>
      <main id="main" class="page-main" tabindex="-1">
        <slot />
      </main>
      <Footer lang={lang} />
    </div>
  </body>
</html>
